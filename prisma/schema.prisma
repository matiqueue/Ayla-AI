datasource db {
  provider = "sqlite"
  url      = "file:./../database/dev.db"
}

generator client {
  provider = "prisma-client-js"
}

model License {
  id         String   @id
  email      String   @unique
  period     String
  createdAt  DateTime @default(now())
  expiresAt  DateTime
}
